import { get } from 'lodash';

import { GENERAL_CONSTANTS, HTTP_ERRORS } from './constants';
import { UserDetails, UserLoginDetails, LoggedInUserDetails } from '../lib/types';

export async function fetchCategories(values: UserDetails): Promise<void> {
    try {
        console.log("Fetch Stats")
    } catch (error) {
        throw new Error(error)
    }
}

export async function fetchCurrencies(details: UserLoginDetails): Promise<void> {
    try {
        console.log("Fetch Stats")
    } catch (error) {
        throw new Error(error)
    }
}

export async function fetchEvents(details: UserLoginDetails): Promise<void> {
    try {
        console.log("Fetch Stats")
    } catch (error) {
        throw new Error(error)
    }
}

export async function fetchItems(details: UserLoginDetails): Promise<void> {
    try {
        console.log("Fetch Stats")
    } catch (error) {
        throw new Error(error)
    }
}

export async function fetchMilestones(details: UserLoginDetails): Promise<void> {
    try {
        console.log("Fetch Stats")
    } catch (error) {
        throw new Error(error)
    }
}

export async function fetchUsers(details: UserLoginDetails): Promise<void> {
    try {
        console.log("Fetch Stats")
    } catch (error) {
        throw new Error(error)
    }
}